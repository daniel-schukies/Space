package ships;

import java.awt.Dimension;

public class Alien extends Ship {

	public Alien(Dimension space, int[] mousePosition, int x, int y) {
		super(space, mousePosition, x, y);
		
/*
	        ImageIcon ii = new ImageIcon(this.getClass().getResource("/resource/gegner.gif"));
	        image = ii.getImage().getScaledInstance(50, 50, 0);
	        width = 50;
	        height = 50;
	        System.out.println("" +image.getHeight(null));
	        visible = true;
	        this.x = x;
	        this.y = y;
	        this.space = space;
	*/    
		

	}

}